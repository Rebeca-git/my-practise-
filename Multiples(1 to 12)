#include <stdio.h>
int main() {
    int num, i;
    printf("Enter a number: ");
    scanf("%d", &num);
    printf("Multiples of %d from 1 to 12:\n", num);
    for(i = 1; i <= 12; i++) {
        printf("%d * %d = %d\n", num, i, num * i);
    }
    return 0;
}
